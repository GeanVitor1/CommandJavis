<Window x:Class="JarvisComando.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:JarvisComando"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Jarvis Comando"
        Width="860" Height="620"
        MinWidth="720" MinHeight="500"
        WindowStyle="None"
        Background="{DynamicResource BgBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}"
        FontFamily="Segoe UI Variable Text, Segoe UI"
        FontSize="13"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize">
    <Window.Resources>
        <DataTemplate x:Key="CommandCardTemplate">
            <Border Style="{StaticResource CommandCard}" PreviewMouseLeftButtonDown="Card_MouseLeftButtonDown"
                    Focusable="True" KeyDown="Card_KeyDown">
                <Grid Margin="14,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <local:AppIconControl Grid.Column="0"
                                          Icon="{Binding Icon}" HasIcon="{Binding HasIcon}"
                                          AvatarChar="{Binding AvatarChar}" Category="{Binding Category}"
                                          Size="36" Margin="0,0,12,0" VerticalAlignment="Center"/>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center">
                        <TextBlock Text="{Binding Description}" FontSize="13.5" FontWeight="SemiBold"/>
                        <TextBlock Text="{Binding Target}" FontSize="11.5" Foreground="{DynamicResource TextSecondaryBrush}" TextTrimming="CharacterEllipsis" Margin="0,2,10,0"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="0,0,14,0">
                        <ItemsControl ItemsSource="{Binding ComboParts}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource Keycap}" Margin="2,0,0,0">
                                        <TextBlock Text="{Binding}" FontWeight="SemiBold" FontSize="11.5" HorizontalAlignment="Center"/>
                                    </Border>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <TextBlock Text="Sem atalho" Foreground="{DynamicResource TextSecondaryBrush}" FontStyle="Italic" FontSize="11.5">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding HasCombo}" Value="False">
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>

                    <StackPanel Grid.Column="3" Orientation="Horizontal" VerticalAlignment="Center">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsMouseOver, RelativeSource={RelativeSource AncestorType=Border}}" Value="True">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <Button Style="{StaticResource RowActionButton}" Content="Abrir" Click="Run_Click"/>
                        <Button Style="{StaticResource RowActionButton}" Content="Editar" Click="Edit_Click"/>
                        <Button Style="{StaticResource RowDangerButton}" Content="Excluir" Click="Remove_Click"/>
                    </StackPanel>
                </Grid>
            </Border>
        </DataTemplate>
    </Window.Resources>

    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="6" GlassFrameThickness="0" CornerRadius="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="42"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- ===== Barra de tÃ­tulo ===== -->
            <Border Grid.Row="0" Background="{DynamicResource TitleBarBrush}" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="14,0,0,0">
                        <Border Width="22" Height="22" CornerRadius="7" Background="{DynamicResource AccentGradientBrush}">
                            <TextBlock Text="J" Foreground="White" FontWeight="Bold" FontSize="12" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Text="Jarvis Comando" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" VerticalAlignment="Center" Margin="8,0,0,0"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                        <Button Style="{StaticResource TitleBarButton}" Content="&#xE921;" Click="Minimize_Click" ToolTip="Minimizar"/>
                        <Button Style="{StaticResource TitleBarCloseButton}" Content="&#xE8BB;" Click="Close_Click" ToolTip="Fechar"/>
                    </StackPanel>
                </Grid>
            </Border>

            <!-- ===== CabeÃ§alho ===== -->
            <Grid Grid.Row="1" Margin="28,22,28,16">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="Seus atalhos" FontSize="22" FontWeight="SemiBold"/>
                    <TextBlock Text="Pressione a combinaÃ§Ã£o em qualquer lugar do Windows" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12.5" Margin="0,4,0,0"/>
                </StackPanel>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <Border Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="13" Padding="12,6">
                        <TextBlock x:Name="StatsText" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11.5"/>
                    </Border>
                    <Button Style="{StaticResource PrimaryButton}" Content="+  Novo comando" Margin="10,0,0,0" Click="Add_Click"/>
                    <Button Style="{StaticResource GhostButton}" Content="Configurações" Margin="10,0,0,0" Click="Settings_Click"/>
                </StackPanel>
            </Grid>

            <!-- ===== Abas + busca ===== -->
            <Grid Grid.Row="2" Margin="28,0,28,16">
                <Border Background="{DynamicResource SurfaceBrush}" CornerRadius="11" Padding="3" HorizontalAlignment="Left">
                    <StackPanel Orientation="Horizontal">
                        <RadioButton x:Name="AppTabRadio" Style="{StaticResource TabPill}" Content="Aplicativos" IsChecked="True" Checked="Tab_Checked"/>
                        <RadioButton x:Name="SiteTabRadio" Style="{StaticResource TabPill}" Content="Sites" Checked="Tab_Checked"/>
                    </StackPanel>
                </Border>
<Grid Width="230" HorizontalAlignment="Right">
                    <local:WatermarkTextBox x:Name="SearchBox" Placeholder="Buscar..." TextChanged="Search_TextChanged"/>
                </Grid>
            </Grid>

<!-- ===== Lista ===== -->
            <Grid Grid.Row="3" Margin="28,0,28,12">
                <ListBox x:Name="AppList"
                         ItemTemplate="{StaticResource CommandCardTemplate}"
                         ItemContainerStyle="{StaticResource ListCardItem}"
                         Background="Transparent" BorderThickness="0"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.CanContentScroll="True"
                         VirtualizingPanel.IsVirtualizing="True"
                         VirtualizingPanel.VirtualizationMode="Recycling"
                         KeyboardNavigation.DirectionalNavigation="Contained">
                </ListBox>

                <StackPanel x:Name="EmptyApp" HorizontalAlignment="Center" Margin="0,64,0,0">
                    <TextBlock Text="Nenhum aplicativo por aqui" FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                    <TextBlock Text="Clique em + Novo comando para adicionar" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" HorizontalAlignment="Center" Margin="0,6,0,0"/>
                </StackPanel>

                <StackPanel x:Name="EmptySite" HorizontalAlignment="Center" Margin="0,64,0,0">
                    <TextBlock Text="Nenhum site por aqui" FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                    <TextBlock Text="Clique em + Novo comando para adicionar" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" HorizontalAlignment="Center" Margin="0,6,0,0"/>
                </StackPanel>

                <StackPanel x:Name="EmptySearch" HorizontalAlignment="Center" Margin="0,64,0,0">
                    <TextBlock Text="Nenhum resultado" FontSize="16" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                    <TextBlock x:Name="EmptySearchText" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="12" HorizontalAlignment="Center" Margin="0,6,0,0"/>
                </StackPanel>
            </Grid>

<!-- ===== RodapÃ© ===== -->
            <Border Grid.Row="4" Background="{DynamicResource TitleBarBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="0,1,0,0" Padding="20,12">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <Button x:Name="MicButton" Style="{StaticResource MicButton}" Content="&#xE720;" ToolTip="Segure para falar"
                            PreviewMouseLeftButtonDown="MicButton_MouseLeftButtonDown"
                            PreviewMouseLeftButtonUp="MicButton_MouseLeftButtonUp"
                            LostMouseCapture="MicButton_LostMouseCapture">
                        <Button.RenderTransform>
                            <ScaleTransform ScaleX="1" ScaleY="1"/>
                        </Button.RenderTransform>
                    </Button>

                    <StackPanel Grid.Column="1" Margin="16,0,0,0" VerticalAlignment="Center">
                        <TextBlock Text="Comando por voz" FontSize="12.5" FontWeight="SemiBold"/>
                        <TextBlock x:Name="VoiceStatusText" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11.5" Margin="0,3,0,0" TextTrimming="CharacterEllipsis"/>
                        <Button x:Name="EnableSpeechButton" Style="{StaticResource GhostButton}" Content="Abrir configurações de fala" FontSize="11" Padding="10,4" Margin="0,6,0,0" HorizontalAlignment="Left" Visibility="Collapsed" Click="EnableSpeech_Click"/>
                    </StackPanel>

                    <StackPanel Grid.Column="2" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="Duplo clique abre o comando" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11.5"/>
                        <TextBlock Text="Fechar minimiza para a bandeja" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11.5" Margin="18,0,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>
    </Border>
</Window>


