<Window x:Class="Vox.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:shell="clr-namespace:System.Windows.Shell;assembly=PresentationFramework"
        Title="Configurações"
        Width="600" Height="660"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStartupLocation="CenterOwner"
        Background="{DynamicResource BgBrush}"
        Foreground="{DynamicResource TextPrimaryBrush}"
        FontFamily="Segoe UI Variable Text, Segoe UI"
        FontSize="13">
    <shell:WindowChrome.WindowChrome>
        <shell:WindowChrome CaptionHeight="0" ResizeBorderThickness="0" GlassFrameThickness="0" CornerRadius="0" UseAeroCaptionButtons="False"/>
    </shell:WindowChrome.WindowChrome>

    <Window.Resources>
        <Style x:Key="StepHeader" TargetType="StackPanel">
            <Setter Property="Orientation" Value="Horizontal"/>
            <Setter Property="Margin" Value="0,0,0,8"/>
        </Style>
        <Style x:Key="StepNumber" TargetType="Border">
            <Setter Property="Width" Value="24"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="CornerRadius" Value="12"/>
            <Setter Property="Background" Value="{DynamicResource SurfaceBrush}"/>
            <Setter Property="BorderBrush" Value="{DynamicResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
        </Style>
        <Style x:Key="StepLabel" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="{DynamicResource TextPrimaryBrush}"/>
            <Setter Property="Margin" Value="10,0,0,0"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>
        <Style x:Key="StepHint" TargetType="TextBlock">
            <Setter Property="FontSize" Value="11"/>
            <Setter Property="Foreground" Value="{DynamicResource TextSecondaryBrush}"/>
            <Setter Property="Margin" Value="0,6,0,0"/>
            <Setter Property="TextWrapping" Value="Wrap"/>
            <Setter Property="Opacity" Value="0.95"/>
            <Setter Property="LineHeight" Value="17"/>
        </Style>
    </Window.Resources>

    <Border BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="12" SnapsToDevicePixels="True">
        <Border.Effect>
            <DropShadowEffect BlurRadius="22" ShadowDepth="6" Opacity="0.14" Color="#000000"/>
        </Border.Effect>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="42"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="{DynamicResource TitleBarBrush}" CornerRadius="12,12,0,0" SnapsToDevicePixels="True">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="16,0,0,0">
                        <Border Width="22" Height="22" CornerRadius="11" Background="{DynamicResource AccentGhostBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1"><TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" FontSize="10" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/></Border>
                        <TextBlock Text="Configurações" Foreground="{DynamicResource TextPrimaryBrush}" FontWeight="SemiBold" FontSize="12" Margin="9,0,0,0" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button Style="{StaticResource TitleBarCloseButton}" Content="&#xE8BB;" HorizontalAlignment="Right" Click="Close_Click" ToolTip="Fechar"/>
                </Grid>
            </Border>

            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="28,20,28,14">

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <Border Width="32" Height="32" CornerRadius="9" Background="{DynamicResource AccentBrush}" SnapsToDevicePixels="True">
                            <TextBlock Text="&#xE713;" FontFamily="Segoe MDL2 Assets" Foreground="White" FontSize="15" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel Margin="12,0,0,0" VerticalAlignment="Center">
                            <TextBlock Text="Configurações" FontWeight="Bold" FontSize="18" Foreground="{DynamicResource TextPrimaryBrush}" TextOptions.TextFormattingMode="Ideal"/>
                            <TextBlock Text="Personalize o Vox do seu jeito" FontSize="11.5" Foreground="{DynamicResource TextTertiaryBrush}" Margin="0,2,0,0"/>
                        </StackPanel>
                    </StackPanel>

                    <StackPanel Style="{StaticResource StepHeader}">
                        <Border Style="{StaticResource StepNumber}">
                            <TextBlock Text="1" FontWeight="Bold" FontSize="11.5" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Style="{StaticResource StepLabel}" Text="Assistente de voz"/>
                    </StackPanel>
                    <StackPanel Margin="0,0,0,0">
                        <CheckBox x:Name="EnableVoiceCheck" Content="Habilitar comando de voz" Checked="EnableVoice_Changed" Unchecked="EnableVoice_Changed"/>
                        <CheckBox x:Name="WakeWordCheck" Content="Ouvir sempre — diga “Ei Vox” para chamar" Margin="0,10,0,0" ToolTip="Fica escutando em segundo plano; ao ouvir “Ei Vox”, ele abre a captura de voz."/>
                        <CheckBox x:Name="AutoStartCheck" Content="Iniciar com o Windows" Margin="0,10,0,0"/>
                        <TextBlock Style="{StaticResource StepHint}" Text="Reconhecimento offline, do jeito Vox."/>
                    </StackPanel>

                    <Border Height="1" Background="{DynamicResource BorderBrush}" Opacity="0.55" Margin="0,16,0,16"/>

                    <StackPanel Style="{StaticResource StepHeader}">
                        <Border Style="{StaticResource StepNumber}">
                            <TextBlock Text="2" FontWeight="Bold" FontSize="11.5" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Style="{StaticResource StepLabel}" Text="Segure para falar"/>
                        <TextBlock Text="tecla global — funciona de qualquer lugar" FontSize="11" Foreground="{DynamicResource TextSecondaryBrush}" Margin="8,0,0,0" VerticalAlignment="Center" Opacity="0.9"/>
                    </StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Border Style="{StaticResource KeycapLarge}" VerticalAlignment="Center">
                            <TextBlock x:Name="HotkeyDisplay" Text="F9" FontWeight="SemiBold" FontSize="14" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="{DynamicResource TextPrimaryBrush}"/>
                        </Border>
                        <Button x:Name="HotkeyButton" Grid.Column="1" Style="{StaticResource GhostButton}" Content="Capturar tecla" Margin="12,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Center" Padding="16,7" Click="CaptureKey_Click"/>
                    </Grid>
                    <Border x:Name="CaptureHintBorder" Visibility="Collapsed" Margin="0,10,0,0" Background="{DynamicResource SelectionBrush}" BorderBrush="{DynamicResource AccentBrush}" BorderThickness="1" CornerRadius="8" Padding="10,7">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE7BA;" FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Center"/>
                            <TextBlock x:Name="CaptureHint" Text="Pressione a tecla desejada (ou Esc para cancelar)" Foreground="{DynamicResource AccentBrush}" FontSize="11.5" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>
                    <TextBlock Style="{StaticResource StepHint}" Text="Pressione e segure para falar com o Vox de qualquer lugar."/>

                    <Border Height="1" Background="{DynamicResource BorderBrush}" Opacity="0.55" Margin="0,16,0,16"/>

                    <StackPanel Style="{StaticResource StepHeader}">
                        <Border Style="{StaticResource StepNumber}">
                            <TextBlock Text="3" FontWeight="Bold" FontSize="11.5" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Style="{StaticResource StepLabel}" Text="Microfone"/>
                    </StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <ComboBox x:Name="MicrophoneCombo" VerticalContentAlignment="Center" MinHeight="36" SelectionChanged="Microphone_SelectionChanged"/>
                        <Button x:Name="RefreshMicrophonesButton" Grid.Column="1" Style="{StaticResource GhostButton}" Margin="10,0,0,0" Padding="13,7" Click="RefreshMicrophones_Click" ToolTip="Atualizar lista">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="&#xE72C;" FontFamily="Segoe MDL2 Assets" FontSize="11" VerticalAlignment="Center" Foreground="{DynamicResource TextSecondaryBrush}"/>
                                <TextBlock Text="Atualizar" Margin="6,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Button>
                    </Grid>
                    <TextBlock Style="{StaticResource StepHint}" Text="Use “Padrão do Windows” para seguir o microfone padrão do sistema. Útil quando você tem um mic virtual como o AudioRelay."/>
                    <Button Style="{StaticResource GhostButton}" Margin="0,10,0,0" HorizontalAlignment="Left" Padding="13,7" Click="TestVoice_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE8F2;" FontFamily="Segoe MDL2 Assets" FontSize="12" VerticalAlignment="Center" Foreground="{DynamicResource AccentBrush}"/>
                            <TextBlock Text="Testar voz de resposta" Margin="7,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>

                    <Border Height="1" Background="{DynamicResource BorderBrush}" Opacity="0.55" Margin="0,16,0,16"/>

                    <StackPanel Style="{StaticResource StepHeader}">
                        <Border Style="{StaticResource StepNumber}">
                            <TextBlock Text="4" FontWeight="Bold" FontSize="11.5" Foreground="{DynamicResource AccentBrush}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <TextBlock Style="{StaticResource StepLabel}" Text="Aparência"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <RadioButton x:Name="ThemeDark" Style="{StaticResource TypePill}" Content="Escuro"/>
                        <RadioButton x:Name="ThemeLight" Style="{StaticResource TypePill}" Content="Claro"/>
                        <RadioButton x:Name="ThemeSystem" Style="{StaticResource TypePill}" Content="Sistema" IsChecked="True"/>
                    </StackPanel>
                    <TextBlock Style="{StaticResource StepHint}" Text="Tema do aplicativo — siga o sistema ou escolha manualmente."/>

                    <Border x:Name="StatusBorder" Background="{DynamicResource SurfaceBrush}" BorderBrush="{DynamicResource BorderBrush}" BorderThickness="1" CornerRadius="10" Padding="12,10" Margin="0,16,0,0" Visibility="Collapsed">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE946;" FontFamily="Segoe MDL2 Assets" FontSize="12" Foreground="{DynamicResource AccentBrush}" VerticalAlignment="Center"/>
                            <TextBlock x:Name="StatusText" Foreground="{DynamicResource TextSecondaryBrush}" FontSize="11.5" Margin="8,0,0,0" TextWrapping="Wrap" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Border>

                </StackPanel>
            </ScrollViewer>

            <Grid Grid.Row="2" Margin="28,0,28,18">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="As alterações são aplicadas ao salvar" FontSize="11.5" Foreground="{DynamicResource TextSecondaryBrush}" VerticalAlignment="Center" TextWrapping="Wrap" Opacity="0.9"/>
                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center">
                    <Button Style="{StaticResource GhostButton}" Content="Cancelar" Padding="16,8" Click="Cancel_Click"/>
                    <Button Style="{StaticResource PrimaryButton}" Content="Salvar" Margin="10,0,0,0" Padding="22,8" Click="Save_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
